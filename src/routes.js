// Routes.js

// Global
const HOME = "/";
const JOIN = "/join";
const LOGIN = "/login";

// Feed
const FEEDS = "/feeds";
const FEEDS_MAIN = "/main";

const routes = {
  home: HOME,
  join: JOIN,
  login: LOGIN,
  feeds: FEEDS,
  feedsMain: FEEDS_MAIN
};

export default routes;
